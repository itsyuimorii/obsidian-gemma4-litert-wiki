{
  "name": "litert-spike",
  "version": "0.0.1",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "check:release": "node scripts/check-release.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.1",
    "obsidian": "latest",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@litert-lm/core": "^0.15.0"
  }
}
